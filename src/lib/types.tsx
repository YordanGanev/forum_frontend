export interface UserType {
  email: string;
  username: string;
  name: string;
  role: string;
}

export interface TopicType {
  id: string;
  title: string;
  username: string;
  created: string;
  modified: string;
  views: number;
}
